#pragma once
#include <iostream>
#include <vector>
#include <chrono>
#include <ctime>
#include <random>
#include <future>

using namespace std;

void merge(vector <int>&, int, int);

void MergeSort(vector <int>&, int, int);

void MergeSortThread(vector<int>& arr, int first, int last);

void MergeSortOne(vector<int>&, int, int);



